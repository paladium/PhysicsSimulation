import { InlineEditorModel } from '@/models/editor';

export const basicShapes = <InlineEditorModel>{
    title: "Drawing basic shapes",
    description: "You can use commands like to draw a square",
    code: `
    let a = draw();
    `
};